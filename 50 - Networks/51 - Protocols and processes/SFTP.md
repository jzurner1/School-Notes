Secure file transfer protocol is a protocol that provides file transferring over an SSH connection. It is the secure version of TCP and uses TCP port 22, just like SSH.

Like scp, it also uses OpenSSH.