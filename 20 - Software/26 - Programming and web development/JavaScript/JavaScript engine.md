A JavaScript engine is how browsers execute [[JavaScript|JavaScript]] code.

Most browsers have their own engine. For example, FireFox uses an engine called SpiderMonkey while Chrome uses one called v8.